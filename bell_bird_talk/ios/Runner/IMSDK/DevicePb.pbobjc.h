// Generated by the protocol buffer compiler.  DO NOT EDIT!
// source: device_pb.proto

// This CPP symbol can be defined to use imports that match up to the framework
// imports needed when using CocoaPods.
#if !defined(GPB_USE_PROTOBUF_FRAMEWORK_IMPORTS)
 #define GPB_USE_PROTOBUF_FRAMEWORK_IMPORTS 0
#endif

#if GPB_USE_PROTOBUF_FRAMEWORK_IMPORTS
 #import <Protobuf/GPBProtocolBuffers.h>
#else
 #import "GPBProtocolBuffers.h"
#endif

#if GOOGLE_PROTOBUF_OBJC_VERSION < 30004
#error This file was generated by a newer version of protoc which is incompatible with your Protocol Buffer library sources.
#endif
#if 30004 < GOOGLE_PROTOBUF_OBJC_MIN_SUPPORTED_VERSION
#error This file was generated by an older version of protoc which is incompatible with your Protocol Buffer library sources.
#endif

// @@protoc_insertion_point(imports)

#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"

CF_EXTERN_C_BEGIN

@class Device;
@class Page;

NS_ASSUME_NONNULL_BEGIN

#pragma mark - Enum DeviceType

/** 设备类型枚举 - 定义支持的设备平台 */
typedef GPB_ENUM(DeviceType) {
  /**
   * Value used if any message's field encounters a value that is not defined
   * by this enum. The message will also have C functions to get/set the rawValue
   * of the field.
   **/
  DeviceType_GPBUnrecognizedEnumeratorValue = kGPBUnrecognizedEnumeratorValue,
  /** Android 设备（默认值） */
  DeviceType_Android = 0,

  /** iOS 设备 */
  DeviceType_Ios = 1,

  /** PC 桌面端 */
  DeviceType_Pc = 2,

  /** Web 浏览器端 */
  DeviceType_Web = 3,
};

GPBEnumDescriptor *DeviceType_EnumDescriptor(void);

/**
 * Checks to see if the given value is defined by the enum or was not known at
 * the time this source was generated.
 **/
BOOL DeviceType_IsValidValue(int32_t value);

#pragma mark - Enum DeviceStatus

/** 设备状态枚举 - 定义设备的各种状态 */
typedef GPB_ENUM(DeviceStatus) {
  /**
   * Value used if any message's field encounters a value that is not defined
   * by this enum. The message will also have C functions to get/set the rawValue
   * of the field.
   **/
  DeviceStatus_GPBUnrecognizedEnumeratorValue = kGPBUnrecognizedEnumeratorValue,
  /** 活跃状态（正常使用中，默认值） */
  DeviceStatus_Active = 0,

  /** 不活跃状态（长时间未使用） */
  DeviceStatus_Inactive = 1,

  /** 被禁用状态（违规或安全问题） */
  DeviceStatus_Banned = 2,

  /** 已归档状态（用户主动移除） */
  DeviceStatus_Archived = 3,
};

GPBEnumDescriptor *DeviceStatus_EnumDescriptor(void);

/**
 * Checks to see if the given value is defined by the enum or was not known at
 * the time this source was generated.
 **/
BOOL DeviceStatus_IsValidValue(int32_t value);

#pragma mark - DevicePbRoot

/**
 * Exposes the extension registry for this file.
 *
 * The base class provides:
 * @code
 *   + (GPBExtensionRegistry *)extensionRegistry;
 * @endcode
 * which is a @c GPBExtensionRegistry that includes all the extensions defined by
 * this file and all files that it depends on.
 **/
GPB_FINAL @interface DevicePbRoot : GPBRootObject
@end

#pragma mark - Device

typedef GPB_ENUM(Device_FieldNumber) {
  Device_FieldNumber_DeviceId = 1,
  Device_FieldNumber_DeviceName = 2,
  Device_FieldNumber_DeviceType = 3,
  Device_FieldNumber_Status = 4,
  Device_FieldNumber_AppVersion = 5,
  Device_FieldNumber_ClientIp = 6,
  Device_FieldNumber_LastActiveTime = 7,
  Device_FieldNumber_CreatedAt = 8,
  Device_FieldNumber_UpdatedAt = 9,
  Device_FieldNumber_SystemName = 10,
  Device_FieldNumber_SystemVersion = 11,
  Device_FieldNumber_KernelVersion = 12,
  Device_FieldNumber_ApiLevel = 13,
  Device_FieldNumber_Brand = 14,
  Device_FieldNumber_Model = 15,
  Device_FieldNumber_Hardware = 16,
  Device_FieldNumber_Board = 17,
  Device_FieldNumber_Product = 18,
  Device_FieldNumber_Serial = 19,
  Device_FieldNumber_ScreenResolution = 20,
  Device_FieldNumber_ScreenDensity = 21,
  Device_FieldNumber_ScreenSize = 22,
  Device_FieldNumber_CpuInfo = 23,
  Device_FieldNumber_MemoryInfo = 24,
  Device_FieldNumber_StorageInfo = 25,
  Device_FieldNumber_GpuInfo = 26,
  Device_FieldNumber_NetworkType = 27,
  Device_FieldNumber_Carrier = 28,
  Device_FieldNumber_WifiSsid = 29,
  Device_FieldNumber_MacAddress = 30,
  Device_FieldNumber_BatteryStatus = 31,
  Device_FieldNumber_BatteryLevel = 32,
  Device_FieldNumber_Timezone = 33,
  Device_FieldNumber_Language = 34,
  Device_FieldNumber_Region = 35,
  Device_FieldNumber_Extra = 36,
};

/**
 * 设备信息 - 核心领域对象
 * 用于设备的基本信息存储和传输
 **/
GPB_FINAL @interface Device : GPBMessage

/** ========== 基础信息（必填） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

/** 设备名称（必填，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceName;

/** 设备类型（必填） */
@property(nonatomic, readwrite) DeviceType deviceType;

/** 设备状态（必填） */
@property(nonatomic, readwrite) DeviceStatus status;

/** ========== 应用信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *appVersion;

/** 客户端IP（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *clientIp;

/** 最后活跃时间戳（可选） */
@property(nonatomic, readwrite) int64_t lastActiveTime;

/** 创建时间戳（系统自动生成） */
@property(nonatomic, readwrite) int64_t createdAt;

/** 更新时间戳（系统自动更新） */
@property(nonatomic, readwrite) int64_t updatedAt;

/** ========== 系统信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *systemName;

/** 系统版本（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *systemVersion;

/** 内核版本（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *kernelVersion;

/** API级别（可选，最大长度10字符，Android专用） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *apiLevel;

/** ========== 硬件信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *brand;

/** 设备型号（可选，最大长度50字符，如：iPhone 14 Pro/Galaxy S23） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *model;

/** 硬件名称（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *hardware;

/** 主板名称（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *board;

/** 产品名称（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *product;

/** 设备序列号（可选，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *serial;

/** ========== 屏幕信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *screenResolution;

/** 屏幕密度（可选，最大长度10字符，单位：dpi） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *screenDensity;

/** 屏幕尺寸（可选，最大长度20字符，如：6.1英寸） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *screenSize;

/** ========== 硬件规格（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *cpuInfo;

/** 内存信息（可选，最大长度50字符，如：8GB） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *memoryInfo;

/** 存储信息（可选，最大长度50字符，如：256GB） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *storageInfo;

/** GPU信息（可选，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *gpuInfo;

/** ========== 网络信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *networkType;

/** 运营商（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *carrier;

/** WiFi名称（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *wifiSsid;

/** MAC地址（可选，最大长度20字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *macAddress;

/** ========== 电池信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *batteryStatus;

/** 电池电量百分比（可选，范围：0-100） */
@property(nonatomic, readwrite) int32_t batteryLevel;

/** ========== 位置信息（可选） ========== */
@property(nonatomic, readwrite, copy, null_resettable) NSString *timezone;

/** 语言（可选，最大长度10字符，如：zh-CN） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *language;

/** 地区（可选，最大长度10字符，如：CN） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *region;

/** ========== 扩展信息（可选） ========== */
@property(nonatomic, readwrite, strong, null_resettable) NSMutableDictionary<NSString*, NSString*> *extra;
/** The number of items in @c extra without causing the array to be created. */
@property(nonatomic, readonly) NSUInteger extra_Count;

@end

/**
 * Fetches the raw value of a @c Device's @c deviceType property, even
 * if the value was not defined by the enum at the time the code was generated.
 **/
int32_t Device_DeviceType_RawValue(Device *message);
/**
 * Sets the raw value of an @c Device's @c deviceType property, allowing
 * it to be set to a value that was not defined by the enum at the time the code
 * was generated.
 **/
void SetDevice_DeviceType_RawValue(Device *message, int32_t value);

/**
 * Fetches the raw value of a @c Device's @c status property, even
 * if the value was not defined by the enum at the time the code was generated.
 **/
int32_t Device_Status_RawValue(Device *message);
/**
 * Sets the raw value of an @c Device's @c status property, allowing
 * it to be set to a value that was not defined by the enum at the time the code
 * was generated.
 **/
void SetDevice_Status_RawValue(Device *message, int32_t value);

#pragma mark - CheckDevice

typedef GPB_ENUM(CheckDevice_FieldNumber) {
  CheckDevice_FieldNumber_UserId = 1,
  CheckDevice_FieldNumber_DeviceId = 2,
  CheckDevice_FieldNumber_Device = 3,
};

/**
 * 检查设备 - 查询操作
 * 用于检查设备是否存在、是否需要认证等
 **/
GPB_FINAL @interface CheckDevice : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

/** 设备信息（可选，用于新设备注册） */
@property(nonatomic, readwrite, strong, null_resettable) Device *device;
/** Test to see if @c device has been set. */
@property(nonatomic, readwrite) BOOL hasDevice;

@end

#pragma mark - GetDevice

typedef GPB_ENUM(GetDevice_FieldNumber) {
  GetDevice_FieldNumber_UserId = 1,
  GetDevice_FieldNumber_DeviceId = 2,
};

/**
 * 获取设备详情 - 查询操作
 * 用于获取指定设备的详细信息
 **/
GPB_FINAL @interface GetDevice : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

@end

#pragma mark - DeviceListQuery

typedef GPB_ENUM(DeviceListQuery_FieldNumber) {
  DeviceListQuery_FieldNumber_UserId = 1,
  DeviceListQuery_FieldNumber_DeviceType = 2,
  DeviceListQuery_FieldNumber_Status = 3,
  DeviceListQuery_FieldNumber_Page = 4,
};

/**
 * 设备列表查询 - 查询操作
 * 用于分页查询用户的设备列表
 **/
GPB_FINAL @interface DeviceListQuery : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备类型过滤（可选） */
@property(nonatomic, readwrite) DeviceType deviceType;

/** 设备状态过滤（可选） */
@property(nonatomic, readwrite) DeviceStatus status;

/** 分页参数（使用系统类型） */
@property(nonatomic, readwrite, strong, null_resettable) Page *page;
/** Test to see if @c page has been set. */
@property(nonatomic, readwrite) BOOL hasPage;

@end

/**
 * Fetches the raw value of a @c DeviceListQuery's @c deviceType property, even
 * if the value was not defined by the enum at the time the code was generated.
 **/
int32_t DeviceListQuery_DeviceType_RawValue(DeviceListQuery *message);
/**
 * Sets the raw value of an @c DeviceListQuery's @c deviceType property, allowing
 * it to be set to a value that was not defined by the enum at the time the code
 * was generated.
 **/
void SetDeviceListQuery_DeviceType_RawValue(DeviceListQuery *message, int32_t value);

/**
 * Fetches the raw value of a @c DeviceListQuery's @c status property, even
 * if the value was not defined by the enum at the time the code was generated.
 **/
int32_t DeviceListQuery_Status_RawValue(DeviceListQuery *message);
/**
 * Sets the raw value of an @c DeviceListQuery's @c status property, allowing
 * it to be set to a value that was not defined by the enum at the time the code
 * was generated.
 **/
void SetDeviceListQuery_Status_RawValue(DeviceListQuery *message, int32_t value);

#pragma mark - getUserByDevice

typedef GPB_ENUM(getUserByDevice_FieldNumber) {
  getUserByDevice_FieldNumber_DeviceId = 1,
};

/**
 * 根据设备ID查询用户 - 查询操作
 * 用于从设备ID反向查询用户信息
 **/
GPB_FINAL @interface getUserByDevice : GPBMessage

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

@end

#pragma mark - updateActivity

typedef GPB_ENUM(updateActivity_FieldNumber) {
  updateActivity_FieldNumber_UserId = 1,
  updateActivity_FieldNumber_DeviceId = 2,
  updateActivity_FieldNumber_ClientIp = 3,
  updateActivity_FieldNumber_ActiveTime = 4,
};

/**
 * 更新设备活跃状态 - 操作对象
 * 用于更新设备的最后活跃时间和IP
 **/
GPB_FINAL @interface updateActivity : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

/** 客户端IP（可选，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *clientIp;

/** 活跃时间戳（可选，不传则使用服务器当前时间） */
@property(nonatomic, readwrite) int64_t activeTime;

@end

#pragma mark - RenameDevice

typedef GPB_ENUM(RenameDevice_FieldNumber) {
  RenameDevice_FieldNumber_UserId = 1,
  RenameDevice_FieldNumber_DeviceId = 2,
  RenameDevice_FieldNumber_NewName = 3,
};

/**
 * 重命名设备 - 操作对象
 * 用于修改设备的显示名称
 **/
GPB_FINAL @interface RenameDevice : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

/** 新设备名称（必填，最大长度50字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *newName NS_RETURNS_NOT_RETAINED;

@end

#pragma mark - authSuccess

typedef GPB_ENUM(authSuccess_FieldNumber) {
  authSuccess_FieldNumber_UserId = 1,
  authSuccess_FieldNumber_DeviceId = 2,
  authSuccess_FieldNumber_Device = 3,
  authSuccess_FieldNumber_CaptchaId = 4,
};

/**
 * 设备认证成功 - 操作对象
 * 用于设备认证通过后的信息记录
 **/
GPB_FINAL @interface authSuccess : GPBMessage

/** 用户ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备ID（必填，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *deviceId;

/** 设备信息（必填） */
@property(nonatomic, readwrite, strong, null_resettable) Device *device;
/** Test to see if @c device has been set. */
@property(nonatomic, readwrite) BOOL hasDevice;

/** 验证码ID（可选，最大长度100字符） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *captchaId;

@end

#pragma mark - checkResult

typedef GPB_ENUM(checkResult_FieldNumber) {
  checkResult_FieldNumber_DeviceExists = 1,
  checkResult_FieldNumber_AuthRequired = 2,
  checkResult_FieldNumber_LimitExceeded = 3,
  checkResult_FieldNumber_CurrentCount = 4,
  checkResult_FieldNumber_MaxCount = 5,
  checkResult_FieldNumber_AuthTokenJwt = 6,
};

/**
 * 设备检查结果 - 结果对象
 * 用于设备检查操作的响应
 **/
GPB_FINAL @interface checkResult : GPBMessage

/** 设备是否存在 */
@property(nonatomic, readwrite) BOOL deviceExists;

/** 是否需要认证 */
@property(nonatomic, readwrite) BOOL authRequired;

/** 是否超过设备数量限制 */
@property(nonatomic, readwrite) BOOL limitExceeded;

/** 当前设备数量 */
@property(nonatomic, readwrite) int32_t currentCount;

/** 最大设备数量 */
@property(nonatomic, readwrite) int32_t maxCount;

/** 认证令牌JWT（需要认证时返回，已签名） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

@end

#pragma mark - DeviceList

typedef GPB_ENUM(DeviceList_FieldNumber) {
  DeviceList_FieldNumber_DevicesArray = 1,
  DeviceList_FieldNumber_Page = 2,
};

/**
 * 设备列表 - 结果对象
 * 用于设备列表查询的响应
 **/
GPB_FINAL @interface DeviceList : GPBMessage

/** 设备列表 */
@property(nonatomic, readwrite, strong, null_resettable) NSMutableArray<Device*> *devicesArray;
/** The number of items in @c devicesArray without causing the array to be created. */
@property(nonatomic, readonly) NSUInteger devicesArray_Count;

/** 分页元数据（使用系统类型） */
@property(nonatomic, readwrite, strong, null_resettable) Page *page;
/** Test to see if @c page has been set. */
@property(nonatomic, readwrite) BOOL hasPage;

@end

#pragma mark - userInfo

typedef GPB_ENUM(userInfo_FieldNumber) {
  userInfo_FieldNumber_UserId = 1,
  userInfo_FieldNumber_Device = 2,
};

/**
 * 用户信息 - 结果对象
 * 用于根据设备查询用户的响应
 **/
GPB_FINAL @interface userInfo : GPBMessage

/** 用户ID */
@property(nonatomic, readwrite, copy, null_resettable) NSString *userId;

/** 设备信息 */
@property(nonatomic, readwrite, strong, null_resettable) Device *device;
/** Test to see if @c device has been set. */
@property(nonatomic, readwrite) BOOL hasDevice;

@end

#pragma mark - GenerateQR

typedef GPB_ENUM(GenerateQR_FieldNumber) {
  GenerateQR_FieldNumber_AuthTokenJwt = 1,
};

/**
 * 生成二维码 - 操作对象
 * 用于生成设备认证二维码
 **/
GPB_FINAL @interface GenerateQR : GPBMessage

/** 认证令牌JWT（必填，从 checkResult 中获取，已签名） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

@end

#pragma mark - QrCode

typedef GPB_ENUM(QrCode_FieldNumber) {
  QrCode_FieldNumber_QrCodeJwt = 1,
  QrCode_FieldNumber_ExpireSeconds = 2,
  QrCode_FieldNumber_QrURL = 3,
};

/**
 * 二维码数据 - 结果对象
 * 用于二维码生成的响应
 **/
GPB_FINAL @interface QrCode : GPBMessage

/** 二维码JWT Token（必填） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *qrCodeJwt;

/** 过期时间（必填，单位：秒） */
@property(nonatomic, readwrite) int32_t expireSeconds;

/** 二维码图片URL（可选，用于直接显示） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *qrURL;

@end

#pragma mark - ScanQR

typedef GPB_ENUM(ScanQR_FieldNumber) {
  ScanQR_FieldNumber_QrCodeJwt = 1,
};

/**
 * 扫描二维码 - 操作对象
 * 用于扫描设备认证二维码
 **/
GPB_FINAL @interface ScanQR : GPBMessage

/** 二维码JWT Token（必填） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *qrCodeJwt;

@end

#pragma mark - SubmitCode

typedef GPB_ENUM(SubmitCode_FieldNumber) {
  SubmitCode_FieldNumber_AuthTokenJwt = 1,
  SubmitCode_FieldNumber_Code = 2,
};

/**
 * 提交验证码 - 操作对象
 * 用于提交设备认证验证码（新设备输入）
 **/
GPB_FINAL @interface SubmitCode : GPBMessage

/** 认证令牌JWT（必填，已签名） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

/** 验证码（必填，6位数字） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *code;

@end

#pragma mark - GetCode

typedef GPB_ENUM(GetCode_FieldNumber) {
  GetCode_FieldNumber_AuthTokenJwt = 1,
};

/**
 * 获取验证码 - 操作对象
 * 用于主动获取设备认证验证码（新设备请求）
 **/
GPB_FINAL @interface GetCode : GPBMessage

/** 认证令牌JWT（必填，已签名） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

@end

#pragma mark - ConfirmCode

typedef GPB_ENUM(ConfirmCode_FieldNumber) {
  ConfirmCode_FieldNumber_AuthTokenJwt = 1,
  ConfirmCode_FieldNumber_Confirmed = 2,
};

/**
 * 确认验证码 - 操作对象
 * 用于确认设备认证验证码（老设备确认）
 **/
GPB_FINAL @interface ConfirmCode : GPBMessage

/** 认证令牌JWT（必填，已签名） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

/** 是否确认（必填） */
@property(nonatomic, readwrite) BOOL confirmed;

@end

#pragma mark - AuthRequired

typedef GPB_ENUM(AuthRequired_FieldNumber) {
  AuthRequired_FieldNumber_AuthTokenJwt = 1,
  AuthRequired_FieldNumber_AuthMethod = 2,
  AuthRequired_FieldNumber_ExpireMinutes = 3,
  AuthRequired_FieldNumber_Description_p = 4,
};

/**
 * 认证需求 - 结果对象
 * 用于返回设备认证需求信息
 **/
GPB_FINAL @interface AuthRequired : GPBMessage

/** 认证令牌JWT（必填，已签名，客户端只需传递） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authTokenJwt;

/** 认证方式（必填，枚举值：qr_code | input_code | confirm_code） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *authMethod;

/** 过期时间（必填，单位：分钟） */
@property(nonatomic, readwrite) int32_t expireMinutes;

/** 认证说明（可选，用于向用户展示） */
@property(nonatomic, readwrite, copy, null_resettable) NSString *description_p;

@end

NS_ASSUME_NONNULL_END

CF_EXTERN_C_END

#pragma clang diagnostic pop

// @@protoc_insertion_point(global_scope)
